[package]
name = "open_stg_base_lib_wapper"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2.11.0"
thiserror = "2.0.18"

[build-dependencies]
build_scripts = { path = "../build_scripts" }

[lib]
crate-type = ["dylib"]

[target.'cfg(all(target_os = "windows",target_env = "msvc"))'.dependencies]
vc-ltl = "*"
